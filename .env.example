# 监听端口
PORT=3000

# 客户端要带上的 token
STORAGE_TOKEN=replace-me-with-a-long-random-string

# 文件落盘目录（相对 cwd 的路径）
UPLOAD_DIR=./uploads

# 返回给客户端的 URL 拼接基址
# 本地开发: http://localhost:3000
# 部署后:   https://your-domain.com
PUBLIC_BASE_URL=http://localhost:3000

# 单个文件最大字节数（默认 10MB）
MAX_FILE_SIZE=10485760

# 持久化 worker 数量（默认 4）
WORKER_COUNT=4

# 任务队列容量（默认 64）
QUEUE_SIZE=64
